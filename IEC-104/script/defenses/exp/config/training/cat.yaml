# CatBoost training hyperparameters (IEC-104 defense/exp)
# Source: IEC-104/script/defenses/exp/defense_CAT_resdnn_sc.ipynb
# Note: `task_type` (CPU/GPU) and `classes_count` are set at runtime

iterations: 5000
depth: 8
learning_rate: 0.05
l2_leaf_reg: 3
loss_function: MultiClass
eval_metric: TotalF1
od_type: Iter
od_wait: 50
random_seed: 42
thread_count: -1
