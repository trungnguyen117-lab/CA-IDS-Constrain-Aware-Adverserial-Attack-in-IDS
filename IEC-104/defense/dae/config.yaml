# DAE purification hyperparameters for IEC-104
# Architecture
hidden_dim: 256
bottleneck_dim: 64
n_res_blocks: 2
dropout: 0.1

# Training
lr: 1e-3
epochs: 2000
batch_size: 512
noise_factor: 0.3
noise_schedule: [0.1, 0.2, 0.3, 0.5]
