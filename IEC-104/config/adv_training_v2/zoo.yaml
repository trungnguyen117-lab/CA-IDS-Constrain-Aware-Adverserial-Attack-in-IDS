# ZOO AT v2 — stronger than eval (max_iter 30→50, nb_parallel 10→15)
# Black-box attack — compatible with all targets

confidence: 0.0
targeted: false
learning_rate: 0.08
max_iter: 50
binary_search_steps: 3
initial_const: 0.001
abort_early: true
use_resize: false
use_importance: false
nb_parallel: 15
batch_size: 1
variable_h: 0.02
verbose: true
