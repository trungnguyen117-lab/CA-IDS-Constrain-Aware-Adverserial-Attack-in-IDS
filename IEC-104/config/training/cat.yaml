# CatBoost training hyperparameters (IEC-104)
# Source: IEC-104/training/training_baselin_tvae.ipynb
# Note: `task_type` (CPU/GPU) and `classes_count` are set at runtime

iterations: 5000
depth: 8
learning_rate: 0.55
loss_function: MultiClass
eval_metric: TotalF1
od_type: Iter
od_wait: 30
random_seed: 42
