# ResDNN training hyperparameters (IEC-104)
# Source: IEC-104/training/8_multi_method.ipynb
# Architecture: stem(→512) → ResBlock×3(512,256) → head
# Note: `input_dim`, `num_class`, and `device` are set at runtime

# ── Optimiser ──
lr: 0.001
weight_decay: 0.0002

# ── Training schedule ──
batch_size: 1024
max_epochs: 120
patience: 20
