# PGD (Projected Gradient Descent) parameters
# White-box iterative attack — requires differentiable model (ftt only)

eps: 0.2
eps_step: 0.01
max_iter: 200
batch_size: 64
targeted: false
verbose: true
