# HopSkipJump Attack (HSJA) parameters
# Black-box decision-boundary attack — compatible with all targets

# ART HopSkipJump params
batch_size: 64
targeted: false
norm: 2
max_iter: 2
max_eval: 100
init_eval: 10
init_size: 25
verbose: true

# Outer BatchedAttackGenerator params
outer_batch_size: 64
max_retries: 2
timeout: 60
verbose_every: 50
