# LightGBM training parameters (from MODBUS notebook 4)

boosting_type: gbdt
objective: multiclass
learning_rate: 0.15
max_depth: 5
n_estimators: 3000
class_weight: balanced
metric: [multi_logloss, multi_error]
early_stopping_rounds: 50
verbose: -1
