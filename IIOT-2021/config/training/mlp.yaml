# PlainMLP (target) training cfg — mirror MLPModel.DEFAULT_CFG.
# Architecture: Linear(in→h1) → ReLU → Dropout → Linear(h1→h2) → ... → head
h1: 256
h2: 128
h3: 64
dropout: 0.1

# Optim
lr: 0.001
weight_decay: 0.0001

# Loop
batch_size: 64
max_epochs: 60
patience: 10
val_size: 0.1
